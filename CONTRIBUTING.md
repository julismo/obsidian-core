# Contributing

Keep contributions small, written in English, and useful as plain Markdown.

Do not add personal notes, private source material, app configuration, plugins, attachments, credentials, contact data, or generated files. Vault content must not reference external services; repository automation is maintained separately.

Before opening a pull request, run `npm test`, `npm run verify`, and `npm run scan:public-safety`.
